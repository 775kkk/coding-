# task:

Реализуем систему для выполнения миграции данных.
В общем случае миграция выполняется из источника в приёмник, 
при этом данные в процессе проходят через набор преобразований. 
Преобразования должны быть настраиваемыми, их можно комбинировать, 
менять порядок, добавлять новые — и при этом ядро вашей системы менять нельзя. 
Ваша задача — сделать такую архитектуру, 
чтобы поведение системы можно было менять без изменения реализации 
(только за счёт подключения других модулей).

- ввод 
`10; Ivan   Petrov ; ivan@example.com`
- вывод
`{"id":<id>,"firstName":"<firstName>","lastName":"<LASTNAME>","email":"<email>"}`

# Реализация:

`datamigrate\client\DataPool.java` - клиент слушает данные
`datamigrate\dto` - папка дтошек
`dto\AccountDTO.java` - рекорд аккаунта
`datamigrate\parser` - парсер и все для него
`parser\exception` - эксепшены парсера
`exception\ParseException.java` - главный эксепшен парсера

`datamigrate\parser\ParserInterface.java` - интерфейс парсеров
`parser\AccountParser.java` - парсер для аккаунта

# На будущее

- все че джейнериковое помечай не т б говно и тд А НОРМАЛЬНО если DTO то так и пиши DTO или соси бибстера не вкуривая поч чел ругается  !!!

# Проебы

- и ваще ты дурак который не шарит как делать пайплайны гибкие лох
- валидатор данных внутри парсера нужен - я забил мне пофек

- выход (close) сделан на отыбись - парсер все равно парсит команду выхода и пихает ее в дто а ток потом уже выход хахахаха оптионал мб

### - `client\DataPool.java` строка 23 - я ничо не умею и не знаю!!! ! ! ! ! ! !  ! ! ! !!!!!!!!!!!!!!!!

## Учтенные

- пайплайн не гибки А ВООБЩЕ ДЕРЬМО - чел один клиент чел чел чел............
- кетч ложит все к чертям в твоем клиенте
- всегда наследуйся от Exception а не от того что он наследует если только наследников уже не ты сам пишешь
- `"Ivan Petrov".trim().split(" ")` даст пустые элементы. Нужно `split("\\s+")`.
/s - служебная штука чтобы сказать любой пробельный символ
/ - чтобы сказать что пишем служебную чтуку
+ - чтобы сказать что у нас более одного

# Лично мне все нравится тут кроме того как у нас DataPool сделан - я думаю можно лучше и круче но я не умею

## Run

аавы;папап;вывы
Failed to parse line: Name part is incorrect
авав;ававыаыва авыавы;уцаывафы
Unexpected error: For input string: "????"
223; ffff ffffs; ddsdsa
Processed account: AccountDTO[id=223, firstName=ffff, lastName=ffffs, email=ddsdsa]