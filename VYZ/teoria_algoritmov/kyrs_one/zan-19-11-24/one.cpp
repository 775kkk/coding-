#include "stdio.h"
#include "../one.h"

int main(){
    // char str[30] ="aBcD";
	// int len = 0;
	// for(;str[len]!='\0';len++);
	// printf("%i\n", A-a);
	// puts(str);//a=97 A=65
	// str[0]-=32;
	// str[1]+=32;
	// str[2]-=32;
	// str[3]+=32;
//	printf("%i %i",str[0] ,str[1]);
	// puts(str);

    // найти колво вхождений "ab"
   чек оне точка аш
}
