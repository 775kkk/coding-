# Сгенеренное условие - чем меньше прочитаешь тем ты готовей к бою - чем меньше абзацев тебе надо для решения тем ты пизже

У нас есть сущность **Заявление**. В заявлении есть **имя автора**, **текст**, есть список **подписей**, и есть итоговая отметка — **принято** или **отказано**. Дальше: заявление должно пройти через **несколько отделов**, но сколько именно отделов будет — заранее неизвестно, и правила отделов тоже могут меняться. Ваша задача — сделать так, чтобы вы могли добавлять новый отдел, убирать отдел, менять порядок отделов, менять их поведение — и при этом **код ядра**, которое гоняет заявление по отделам, вы не переписываете.

Итак, слушаем. Сейчас делаем задачу не “про одну конкретную проверку”, а про то, чтобы у вас получилась **система**, которую можно перенастраивать без переписывания ядра.

Теперь про то, как заявление “ходит”. Вы получаете заявление, отдаёте его в первый отдел. Отдел либо ставит отметку “принято” или “отказано” и на этом всё заканчивается, либо добавляет свою подпись и передаёт заявление дальше. И как только в каком-то месте у заявления появилась финальная отметка — вы возвращаете результат. Всё.

Отделов для демонстрации делаем три, и правила такие. Первый — **Безопасность**. У вас есть список запрещённых авторов. Если автор в этом списке — безопасность сразу ставит отказ, ничего не подписывает и заявление возвращается. Если автор не запрещён — безопасность добавляет подпись “Безопасность” и передаёт заявление в кадровый отдел.

Дальше идёт **Отдел кадров**. У кадров есть настройка “есть вакансии или нет”. Если вакансии есть — кадры добавляют подпись “отдел кадров” и передают заявление дальше. Если вакансий нет — кадры ставят отказ и возвращают заявление.

Дальше — **Бухгалтерия**. Тут тоньше. Бухгалтерия проверяет две вещи: во-первых, текст заявления должен начинаться **с заглавной буквы**, иначе отказ. Во-вторых, у заявления должна быть подпись отдела кадров. Если подписи кадров нет — бухгалтерия не ставит сразу отказ, она **перенаправляет** заявление обратно в отдел кадров. Если подпись есть и текст начинается с большой буквы — бухгалтерия ставит “принято”, добавляет подпись “Бухгалтерия” и возвращает заявление как итог.

Теперь по вводу-выводу, чтобы можно было проверить. Вы делаете консольную демонстрацию. Сначала пользователь вводит строку `VACANCIES=0` или `VACANCIES=1`, то есть есть вакансии или нет. Потом вводит строку `BANNED=...` со списком запрещённых имён через запятую, может быть пусто. Дальше он вводит сами заявления, каждое заявление одной строкой в формате `AUTHOR|TEXT`. И когда он вводит строку `END`, вы прекращаете ввод.

Для каждого заявления вы прогоняете его через вашу систему отделов и печатаете результат одной строкой: `RESULT=true` или `RESULT=false`, имя автора и список подписей, которые успели набраться. То есть печатаете примерно так: `RESULT=true; AUTHOR=Alice; SIGNS=Безопасность,отдел кадров,Бухгалтерия` — или если отказ, то `RESULT=false` и список подписей, которые были до отказа.

# КАРОЧЕ Я НИЧЕ НЕ ЗНАЮ И НЕ УМЕЮ ПОМОГИТЕ (БОЖЕ МОЙ КАК НАСТРОИТЬ ГИБКО ДЕПАРТАМЕНТЫ 😭😭😭)